interface:
  display_name: "VarCrawl"
  short_description: "Repo-specific patterns and workflows for VarCrawl"
  default_prompt: "Use the VarCrawl repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true